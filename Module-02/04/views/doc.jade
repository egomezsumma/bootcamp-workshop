extends layout

block content
  .page-header
    h1.doc-title= (typeof title == 'undefined' ? 'untitled' : title)
  .row-fluid(style='height: 85%;')
    textarea#editor.span6= content

  script
    window.docId = "#{_id.toString()}";